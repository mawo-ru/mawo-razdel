# Руководство по внесению вклада в mawo-razdel

Спасибо за интерес к улучшению mawo-razdel! Мы приветствуем вклад от сообщества.

## Как внести вклад

### Сообщение о проблемах

Если вы нашли баг или хотите предложить новую функцию:

1. Проверьте, нет ли уже существующего issue
2. Создайте новый issue с подробным описанием:
   - Для багов: шаги воспроизведения, ожидаемое и фактическое поведение
   - Для новых функций: описание use case и предлагаемое API

### Pull Requests

1. **Форкните репозиторий** и создайте ветку от `main`:
   ```bash
   git checkout -b feature/my-feature
   ```

2. **Настройте окружение разработки**:
   ```bash
   git clone https://github.com/your-username/mawo-razdel.git
   cd mawo-razdel
   pip install -e ".[dev]"
   ```

3. **Внесите изменения**:
   - Следуйте стилю кода проекта
   - Добавьте тесты для новой функциональности
   - Обновите документацию при необходимости

4. **Запустите тесты и линтеры**:
   ```bash
   # Тесты
   pytest tests/

   # Форматирование кода
   black .

   # Проверка стиля
   ruff check .

   # Проверка типов
   mypy mawo_razdel/
   ```

5. **Создайте Pull Request**:
   - Опишите изменения
   - Ссылайтесь на связанные issues
   - Убедитесь, что все проверки прошли успешно

## Стандарты кода

### Стиль Python

- Следуем PEP 8
- Максимальная длина строки: 100 символов
- Используем type hints для всех публичных функций
- Форматируем код с помощью `black`
- Проверяем с помощью `ruff`

### Документация

- Все публичные функции и классы должны иметь docstrings
- Используем Google-style docstrings:

```python
def function_name(param1: str, param2: int) -> bool:
    """Краткое описание функции.

    Args:
        param1: Описание param1
        param2: Описание param2

    Returns:
        Описание возвращаемого значения
    """
    pass
```

### Тесты

- Пишем тесты для всей новой функциональности
- Используем pytest
- Стремимся к высокому покрытию кода
- Тесты должны быть быстрыми и изолированными

## Добавление новых аббревиатур

Если вы хотите добавить новые аббревиатуры в `syntagrus_patterns.py`:

1. Добавьте аббревиатуру в соответствующую категорию в `ABBREVIATIONS`
2. Добавьте примеры использования в тесты
3. Проверьте, что сегментация работает корректно
4. Укажите источник или контекст использования в описании PR

## Процесс ревью

1. Мейнтейнеры проверят ваш PR
2. Могут попросить внести изменения
3. После одобрения PR будет смержен в `main`
4. Изменения войдут в следующий релиз

## Релизы

Мы следуем [Semantic Versioning](https://semver.org/lang/ru/):

- **MAJOR**: несовместимые изменения API
- **MINOR**: новая функциональность с обратной совместимостью
- **PATCH**: багфиксы с обратной совместимостью

## Вопросы?

Если у вас есть вопросы:

- Создайте issue с меткой "question"
- Напишите в discussions на GitHub

## Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет распространяться под лицензией MIT.

Спасибо за ваш вклад! 🎉
